export type Company = {
  id: string;
  name: string;
  signed_up: string;
};

export type Person = {
  collaborator_invited: number;
  comment_created: number;
  company_id: string;
  document_created: number;
  id: string;
  last_seen: string;
  name: string;
  signed_up: string;
};
